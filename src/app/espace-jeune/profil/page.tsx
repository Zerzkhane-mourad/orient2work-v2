import { ProfileView } from "@/features/jeune/profil/profile-view";

// The profile store is provided by the Espace Jeune layout, so edits made here
// are immediately visible in the header and on the dashboard.
export default function MonProfilPage() {
  return <ProfileView />;
}
